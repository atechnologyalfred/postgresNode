
<%- include("partials/header.ejs") %>

<style>
        button {
            cursor: pointer;
            padding: 0.5rem;
            transition: all 0.5s;
            border: none;
            width: 10rem;
            background-color: #fff;
        }
        button:hover {
            background-color: bisque;
            color: #000;
        }
        .btn {
            margin-right: 1rem;
            background-color: black;
            color: #fff;
        }
        .body {
          position: absolute;
           top: 50%;
           left: 50%;
           transform: translate(-50%, -50%);
           text-align: center;
           font-size: 1.8rem;
           width: 50rem;
           padding: 2rem;
           
        }

      p {
        margin: 1rem 0;
      }
      button {
        margin-top: 1rem;
        border: 1px solid rgb(207, 207, 207);
      }
      h2 {
        letter-spacing: .5rem;
      }
      input {
        cursor: pointer;
        border: none;
        background-color: none;
      }
    </style>
</head>
<body>
  <form action = "/success" method = "post" class = "body">
        <h1>✅ welcome <%= current.first_name %></h1>
        <h2>Your secret code is <span><%= current.password %></span></h2>
        <h2>Account created at <span> <%= current.created_at %></span></h2>
        <p>We are excited you made it here.. enjoy your game</p>
        <input type = "submit" value = "Logout" />
       
  </form>
</body>
</html>